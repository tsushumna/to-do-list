import { CurrentPagePipe } from './current-page.pipe';

describe('CurrentPagePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrentPagePipe();
    expect(pipe).toBeTruthy();
  });
});
